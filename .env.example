# Copy to .env — docker-compose reads this file.
OPENAI_API_KEY=sk-...

# Optional — these all have sensible defaults.
# OPENAI_MODEL=gpt-4o-mini
# ANALYSIS_BATCH_SIZE=5
# Set to false to skip the web-search step during an assessment.
# ENABLE_WEB_SEARCH=true
